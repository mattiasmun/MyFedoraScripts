#!/bin/bash
sleep 0.3

function _closeall {
	for i in {1..9}
	do
		for j in "$(wmctrl -l | awk '{print $1}')"
		do
			sleep 0.42
			xdotool key --clearmodifiers Alt+F4
		done
	done
	sleep 0.42
	if [ "$2" == 1 ]
	then
		systemctl "$1"
	fi
}

_closeall "$1" "$2" &
