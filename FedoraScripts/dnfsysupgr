#!/bin/bash
source yesno
v=$(rpm -E %fedora)
v=$((v+1))

function closeandreboot {
	closeall
	sudo -A dnf system-upgrade reboot
}

sudo dnf -y system-upgrade download --refresh --allowerasing --releasever=$v
echo 'Reboot to perform system upgrade now? [Y/n] '
if confirm
then
	closeandreboot &
fi
