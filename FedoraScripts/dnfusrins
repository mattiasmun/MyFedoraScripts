#!/bin/bash
file=$HOME/Bash/MyFedoraScripts/FedoraScripts/progs
echo '#!/bin/bash' > $file
echo 'dnf install -y \' >> $file
dnf repoquery --userinstalled --queryformat '%{name} \' >> $file
gawk -i inplace -v RS='^$' -v ORS= '{$0=gensub(/\/([^\/]*)$/,"\\1","")}1' $file
