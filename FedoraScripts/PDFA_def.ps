%!
% Definitionsfil för PDF/A-3b
[/Value (3) /T (part) /DIR {systemdict /pdfmark get} cvx /DOCINFO pdfmark
[/Value (B) /T (conformance) /DIR {systemdict /pdfmark get} cvx /DOCINFO pdfmark

% Här kan du lägga till ICC-profil för färgrymd (viktigt för validering)
[/ICCProfile (srgb.icc) /DestOutputProfile {systemdict /pdfmark get} cvx /DESTINATION pdfmark
